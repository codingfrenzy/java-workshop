import java.util.HashMap;
public class LongestPalindrome extends ASolution {
		@Override
    void runSolution() {

    }

    public String reconstruct(String[][] str){
    	/*
    	hashmap destinations
    	push jfk
    	while stack not empty
    		count = 0;
    		while count <= str.length && noOption()
    			count++;
    			current = stack.top()
    			if (!(current + destinations.get(current)).substr(route))
    				route += destinations.get(current)
    				
    	*/
    	return null;
    }
}